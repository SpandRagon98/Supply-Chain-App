# Configurable risk engine

Phase 8 scores incidents with explicit, versioned weights for severity, signal confidence, supplier criticality, single-source dependency, inventory exposure, and revenue exposure. Weights must sum to one and every persisted factor records its raw value, normalization, weight, contribution, and lineage.
