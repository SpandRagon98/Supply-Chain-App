# Recommendations and approvals

Phase 11 transforms the optimizer-selected feasible scenario into an explainable recommendation containing protected revenue and orders, delay, cost, assumptions, risks, confidence, and alternatives. Approval routing uses configurable monetary policy bands, with explicit new-supplier escalation. Each decision is appended as an immutable history row and authorization is enforced against the routed role.
