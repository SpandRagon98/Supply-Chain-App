# Infrastructure

Deployment-specific manifests will live here without coupling the application to a hosting vendor. The target topology supports a separately deployed frontend, FastAPI API/worker, PostgreSQL, Redis, and an AI provider configured through environment variables.
