export function RecordList({ columns, rows }: { columns: { key: string; label: string }[]; rows: Record<string, string | number | null>[] }) {
  return <section className="card panel" style={{ overflowX: "auto" }}><table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13 }}><thead><tr>{columns.map((column) => <th key={column.key} style={{ padding: "0 12px 12px", color: "var(--muted)", textAlign: "left", fontWeight: 700 }}>{column.label}</th>)}</tr></thead><tbody>{rows.map((row, index) => <tr key={String(row.id ?? index)}>{columns.map((column) => <td key={column.key} style={{ padding: "13px 12px", borderTop: "1px solid var(--border)" }}>{row[column.key] ?? "—"}</td>)}</tr>)}</tbody></table></section>;
}
